name: variant
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python>=3.8
  - biopython>=1.79
  - numpy>=1.21.0
  - pandas>=1.3.0
  - pyyaml>=6.0
  - more-itertools>=8.12.0
  - pip
  - pip:
    - fuzzysearch>=0.7.3
prefix: ./envs/variant
